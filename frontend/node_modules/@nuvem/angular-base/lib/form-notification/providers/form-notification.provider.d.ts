import { ApplicationProblemType } from "../../error/types/application-problem.type";
import { FormNotification } from "../types/form-notification.type";
/**
 * Class provider para definição básica de tratamento de erros de formulários
 * @class
 */
export declare class FormNotificationProvider extends ApplicationProblemType {
    violations?: FormNotification[];
    /**
     * Metodo construtor para validação de formulários
     * @param {string} type
     * @param {string} title
     * @param {string} detail
     * @param {Number} status
     * @param {string} instance
     * @param {Array<string>} stacktrace
     * @param {ApplicationProblemType} cause
     * @param {FormViolation} violations
     */
    constructor(type: string | 'about:blank', title: string, detail?: string, status?: Number, instance?: string, stacktrace?: Array<string>, cause?: ApplicationProblemType, violations?: FormNotification[]);
}
