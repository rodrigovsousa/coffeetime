import { FormNotification } from "./form-notification.type";
/**
 * Class modelo para validação de campos de formulários
 * @class
 */
export declare class FieldFormNotification extends FormNotification {
    field: string;
    message: string;
    /**
     * Método construtor para validação de campos de formulários
     * @param {string} form
     * @param {string} field
     * @param {string} message
     */
    constructor(form: string, field: string, message: string);
}
