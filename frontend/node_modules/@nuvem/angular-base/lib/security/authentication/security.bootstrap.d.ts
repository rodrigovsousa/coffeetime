import { AuthConfig } from "../config/auth-config";
import { isAuthenticated as ia, redirect } from "./authentication.service";
export declare function bootstrapSecurity(config: AuthConfig, authenticated: () => void, isAuthenticated?: typeof ia, notAuthenticated?: typeof redirect): void;
