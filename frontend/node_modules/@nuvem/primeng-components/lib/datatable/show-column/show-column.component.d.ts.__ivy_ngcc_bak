import { OnInit, QueryList, EventEmitter } from '@angular/core';
import { PageNotificationService } from './../../page-notification/page-notification.service';
export declare class ShowColumn implements OnInit {
    private pageNotificationService;
    /**
     * atributo que representa as colunas da tabela
     * @type {QueryList}
     */
    columns: QueryList<any>;
    /**
     * lista de colunas disponívels na tabela
     * @type {Array}
     */
    options: any[];
    /**
     * Valores do select de colunas
     * @type {Array}
     */
    values: any[];
    /**
     * Último valor válido para o select.
     * @type {Array}
     */
    private lastValue;
    /**
     * Evento para atualizar a visibilidade das colunas da tabela
     * @type {EventEmitter}
     */
    updateVisibleColumns: EventEmitter<any>;
    constructor(pageNotificationService: PageNotificationService);
    ngOnInit(): void;
    mostrarColunas(event: any): void;
}
